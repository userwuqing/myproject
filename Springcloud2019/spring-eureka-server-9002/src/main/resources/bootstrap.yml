spring:
  cloud:
    config:
      uri: http://localhost:3344 #配置中心uri
      profile: dev
      name: eureka-server9002
      fail-fast: true #快速失败返回
      #request-read-timeout: 5
      #username:  #git用户名
      #password:  #git密码
