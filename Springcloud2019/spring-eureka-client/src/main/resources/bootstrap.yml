spring:
  application:
    name: application-test
  profiles:
    active: dev

  cloud:
    config:
      uri: http://localhost:3344
      fail-fast: true
      #request-read-timeout: 5
      #username:  #git用户名
      #password:  #git密码
      profile: dev
      name: spring-eureka-client

    bus:
      trace:
        enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest