spring:
  application:
    name: application-test

  cloud:
    config:
      uri: http://localhost:3344
      fail-fast: true
      #request-read-timeout: 5
      #username:  #git用户名
      #password:  #git密码
